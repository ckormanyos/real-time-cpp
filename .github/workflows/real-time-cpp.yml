##############################################################################
#  Copyright Christopher Kormanyos 2021 - 2022.
#  Distributed under the Boost Software License,
#  Version 1.0. (See accompanying file LICENSE_1_0.txt
#  or copy at http://www.boost.org/LICENSE_1_0.txt)

name: real-time-cpp
on:
  push:
    branches:
      - '**'
  pull_request:
  schedule:
    - cron: '45 23 * * *' # run at 11:45 PM UTC
jobs:
  target-riscvfe310:
    runs-on: ubuntu-22.04
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        suite: [ riscvfe310 ]
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'
      - name: update-tools
        run: |
          mkdir -p riscv64-unknown-elf && cd riscv64-unknown-elf
          wget https://github.com/stnolting/riscv-gcc-prebuilt/releases/download/rv64imc-3.0.0/riscv64-unknown-elf.gcc-12.1.0.tar.gz
          tar -xvzf ./riscv64-unknown-elf.gcc-12.1.0.tar.gz
        working-directory: ./
      - name: target-riscvfe310-${{ matrix.suite }}
        run: |
          PATH=../riscv64-unknown-elf/bin:$PATH
          ./target/build/build.sh ${{ matrix.suite }} rebuild
          ls -la ./bin/ref_app.elf ./bin/ref_app.hex ./bin/ref_app.map ./bin/ref_app.s19
        working-directory: ./ref_app/
  target-riscvfe310-cmake:
    runs-on: ubuntu-22.04
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      matrix:
        suite: [ riscvfe310 ]
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'
      - name: update-tools
        run: |
          mkdir -p riscv64-unknown-elf && cd riscv64-unknown-elf
          wget https://github.com/stnolting/riscv-gcc-prebuilt/releases/download/rv64imc-3.0.0/riscv64-unknown-elf.gcc-12.1.0.tar.gz
          tar -xvzf ./riscv64-unknown-elf.gcc-12.1.0.tar.gz
        working-directory: ./
      - name: target-riscvfe310-cmake-${{ matrix.suite }}
        run: |
          mkdir -p build && cd build
          PATH=../riscv64-unknown-elf/bin:$PATH
          cmake ../ref_app -DTRIPLE=riscv64-unknown-elf -DTARGET=${{ matrix.suite }} -DCMAKE_TOOLCHAIN_FILE=../ref_app/cmake/gcc-toolchain.cmake
          make -j ref_app
          ls -la ref_app.hex ref_app.s19 ref_app.map
        working-directory: ./
